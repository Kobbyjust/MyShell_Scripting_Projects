1. To Check which shell you are using, Use
    echo $SHELL

2. TO Configure Bash
    which bash
    /usr/bin/bash

# IMPORTANT
Every bash script starts with
#!/bin/bash
This command tells the what interpretor the script uses

## To Execute A Script
1. Make it excutable by using
    sudo chmod +x script_name
